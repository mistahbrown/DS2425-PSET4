#include "tree.h"
#include <vector>

// Returns the maximum path in the tree given by root
int maxPathSum(TreeNode* root) 
{
        
}

// Returns the minimum number of cameras needed to cover the tree
// given by root
int minCameraCover(TreeNode* root) 
{
        
}

// Returns the k closest values to target in the BST given by root
vector<int> closestKValues(TreeNode* root, double target, int k)
{

}